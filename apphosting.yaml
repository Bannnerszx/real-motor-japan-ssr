# Settings for Backend (on Cloud Run).
runConfig:
  minInstances: 0

# Environment variables and secrets
secrets:
  - key: FIREBASE_SERVICE_ACCOUNT
    secret: projects/samplermj/secrets/firebase-service-account/versions/latest