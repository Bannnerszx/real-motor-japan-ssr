# Settings for Backend (on Cloud Run).
runConfig:
  minInstances: 0

# Environment variables and secrets
secrets:
  - key: FIREBASE_SERVICE_ACCOUNT
    secret: projects/879567069316/secrets/firebase-service-account/versions/latest